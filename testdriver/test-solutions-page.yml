version: 4.0.69
steps:
  - prompt: Click on "Solutions" in the navigation menu.
    commands:
      - command: hover-text
        text: Solutions
        description: navigation menu item
        action: click
  - prompt: >-
      Assert that a page listing solutions or case studies loads.  # Use the
      content of the file from the environment
    commands:
      - command: assert
        expect: the page lists solutions or case studies
  - prompt: >-
      Assert that a page listing solutions or case studies loads.  # Use the
      content of the file from the environment
    commands:
      - command: hover-text
        text: Live Audience Engagement
        description: solution option in the dropdown
        action: click
      - command: assert
        expect: the page lists solutions or case studies
